# GitHub Pages Deployment for Phleg Digital Oracle

## Що було підготовлено

Я підготував повну структуру для розгортання сторінки "Phleg Digital Oracle" на GitHub Pages:

### Створені файли та папки:

```
phleg/
├── docs/                          # Папка для GitHub Pages
│   ├── index.html                # Головна сторінка (перейменована з phleg-oracle.html)
│   ├── .nojekyll                 # Вимкнення Jekyll обробки
│   ├── README.md                 # Інструкції розгортання
│   └── CNAME.example             # Приклад файлу для кастомного домену
├── phleg-oracle.html             # Оригінальний HTML файл
└── GITHUB_PAGES_DEPLOYMENT.md    # Цей файл з інструкціями
```

### Оновлення в коді:
- ✅ Оновлено рік у футері з 2024 на 2025
- ✅ Покращено логотип для кращої читабельності
- ✅ Додано адаптивний дизайн для мобільних пристроїв

## Кроки для розгортання на GitHub Pages

### Крок 1: Додати файли до Git

```bash
# Додати папку docs та інші зміни
git add docs/
git add GITHUB_PAGES_DEPLOYMENT.md
git add phleg-oracle.html

# Створити коміт
git commit -m "feat: Add Phleg Digital Oracle landing page for GitHub Pages"

# Запушити зміни
git push origin main
```

### Крок 2: Налаштувати GitHub Pages

1. Перейдіть у ваш репозиторій на GitHub
2. Клікніть **Settings** (Налаштування)
3. У лівому меню виберіть **Pages**
4. У розділі **Build and deployment** (Збірка та розгортання):
   - **Source** (Джерело): Виберіть **Deploy from a branch** (Розгортання з гілки)
   - **Branch** (Гілка): Виберіть **main** (або вашу основну гілку)
   - **Folder** (Папка): Виберіть **/docs**
5. Клікніть **Save** (Зберегти)

### Крок 3: Дочекатися розгортання

- GitHub автоматично розгорне сторінку протягом 1-2 хвилин
- Ваша сторінка буде доступна за адресою:
  ```
  https://[ваш-юзернейм].github.io/[назва-репозиторію]/
  ```
- Статус розгортання можна перевірити в **Actions** (Дії) → **Pages build and deployment**

## Альтернативний спосіб: Гілка `gh-pages`

Якщо ви хочете використовувати окрему гілку:

```bash
# Створити та перейти на гілку gh-pages
git checkout -b gh-pages

# Скопіювати файли з docs в корінь
cp -r docs/* .

# Додати та закомітити
git add .
git commit -m "Deploy Phleg Digital Oracle to GitHub Pages"
git push origin gh-pages

# Повернутися на основну гілку
git checkout main
```

Потім у налаштуваннях GitHub Pages виберіть:
- **Branch** (Гілка): `gh-pages`
- **Folder** (Папка): `/ (root)` (корінь)

## Кастомний домен (опційно)

Якщо хочете використовувати власний домен (наприклад, `phleg.dev`):

1. **Створити файл CNAME**:
   ```bash
   echo "phleg.dev" > docs/CNAME
   # або для піддомену:
   # echo "www.phleg.dev" > docs/CNAME
   ```

2. **Налаштувати DNS** у вашого провайдера домену:
   - Додати **CNAME запис**, що вказує на `[ваш-юзернейм].github.io`
   - АБО додати **A записи** для IP адрес GitHub Pages

3. **У налаштуваннях GitHub Pages**:
   - У розділі **Custom domain** (Кастомний домен) введіть ваш домен
   - Відмітьте **Enforce HTTPS** (Примусово HTTPS)

## Локальне тестування

Перед розгортанням можна протестувати локально:

```bash
# Перейти в папку docs
cd docs

# Запустити локальний сервер
python3 -m http.server 8000

# Відкрити в браузері: http://localhost:8000
```

Або використовуючи Node.js:
```bash
npx http-server docs -p 8000
```

## Оновлення сторінки

Щоб оновити сторінку в майбутньому:

1. Внесіть зміни в оригінальний файл `phleg-oracle.html`
2. Скопіюйте його в `docs/index.html`:
   ```bash
   cp phleg-oracle.html docs/index.html
   ```
3. Закомітьте та запушитіть зміни:
   ```bash
   git add docs/index.html
   git commit -m "Update Phleg landing page"
   git push origin main
   ```
4. GitHub Pages автоматично перерозгорне сторінку

## Поширені проблеми та вирішення

### Сторінка не оновлюється?
- Кеш GitHub Pages може зберігатися кілька хвилин
- Спробуйте примусове оновлення: **Ctrl+F5** (Windows/Linux) або **Cmd+Shift+R** (Mac)
- Перевірте статус розгортання в **Actions**

### Помилка 404?
- Переконайтеся, що `index.html` знаходиться в папці `docs/`
- Перевірте налаштування GitHub Pages (джерело має бути `/docs`)

### GitHub обробляє сторінку через Jekyll?
- Файл `.nojekyll` має запобігати цьому
- Якщо бачите сирий HTML, переконайтеся, що `.nojekyll` присутній у папці `docs/`

### Кастомний домен не працює?
- Зміни DNS можуть поширюватися до 24 годин
- Перевірте правильність CNAME/A записів
- У налаштуваннях GitHub Pages перевірте, чи верифікований домен

## Особливості цієї сторінки

- ✅ **Повністю статична**: Не потребує збірки
- ✅ **Адаптивний дизайн**: Працює на мобільних та десктопах
- ✅ **Унікальний дизайн**: Matrix + Ancient Greek тематика
- ✅ **Інтерактивні елементи**: Анімація дощу коду, ефекти набору тексту
- ✅ **Без зовнішніх залежностей**: Шрифти завантажуються з Google Fonts CDN

## Посилання

- **Локальна копія**: `phleg-oracle.html`
- **Для розгортання**: `docs/index.html`
- **Інструкції**: `docs/README.md` та `GITHUB_PAGES_DEPLOYMENT.md`

---

**Готово до розгортання!** 🚀

Після виконання кроків вище, ваша сторінка буде доступна на GitHub Pages з унікальним дизайном "Digital Oracle".